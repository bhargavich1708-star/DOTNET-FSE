# Microservices Architecture - JWT Authentication
This project demonstrates the implementation of JWT Authentication in an ASP.NET Core Web API microservice.

## Prerequisites
To run this project, you must install the required NuGet package:
`dotnet add package Microsoft.AspNetCore.Authentication.JwtBearer`